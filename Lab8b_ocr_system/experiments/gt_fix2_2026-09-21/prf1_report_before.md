# Lab7B — P/R/F1 + CER/WER รวม 7 หลักสูตร (2026-09-16)

คนละแกนการวัดกับ `lab9_metrics_latest.md` (ไฟล์นั้นวัดฝั่ง Lab8B: conversion_rate, verify_pass_rate,
NL→SQL — คนละสคริปต์, `evaluate_lab9.py`) — ไฟล์นี้วัดคุณภาพขั้น **สกัดข้อมูลดิบของ Lab7B**
(`lab7b_curriculum.py --gt`, เทียบกับ ground truth) โดยใช้ `lab7_metrics.py` ที่เป็นของเดิมจาก
ใบงานอาจารย์ ไม่เคยถูกแก้ (ดู `Lab8b_ocr_system/PROGRESS.md` หัวข้อ 2026-09-15/16)

ground truth ที่ใช้: `Lab9_evaluation/ground_truth_scoped/{run}_scoped.json` (กรอง
`data/ground_truth/*.json` เอาเฉพาะ `year >= 1`) — **ดู caveat ท้ายไฟล์ก่อนอ่านตัวเลขจริงจัง**

**tokenizer สำหรับ WER:** `pythainlp/newmm` (pythainlp 5.3.7, ตัดคำแบบ dictionary-based maximal matching)
— WER ภาษาไทยขึ้นกับ tokenizer โดยตรง (ไม่มีช่องว่างคั่นคำ) ตัวเลข WER ในไฟล์นี้เทียบกับรายงานอื่นได้
เฉพาะที่ใช้ tokenizer เดียวกัน ถ้าไม่มี pythainlp โค้ดจะ fallback ตัดคำด้วยช่องว่างซึ่งได้ WER ที่ผิด
(CER ไม่กระทบเพราะนับระดับตัวอักษร) ตรวจได้จากฟิลด์ `tokenizer` ใน `evaluation.json` แต่ละ run

## สรุปรวม

| run | GT | pred | matched | missed | spurious | P | R | F1 |
|---|---|---|---|---|---|---|---|---|
| ait | 41 | 37 | 31 | 10 | 6 | 0.838 | 0.756 | 0.795 |
| bit_no_coop | 43 | 42 | 38 | 5 | 4 | 0.905 | 0.884 | 0.894 |
| bit_coop | 42 | 45 | 34 | 8 | 11 | 0.756 | 0.809 | 0.782 |
| ours_coop (DSBA-coop) | 45 | 65 | 35 | 10 | 30 | 0.538 | 0.778 | 0.636 |
| dsba_no_coop | 46 | 52 | 33 | 13 | 19 | 0.635 | 0.717 | 0.673 |
| it_no_coop | 54 | 49 | 39 | 15 | 10 | 0.796 | 0.722 | 0.757 |
| it_coop | 53 | 55 | 39 | 14 | 16 | 0.709 | 0.736 | 0.722 |

`bit_no_coop` ดีที่สุด (F1=0.894) — ตารางแผนหน้าเดียวชัดเจน ไม่มี rowspan ซับซ้อน
`ours_coop` แย่สุด (F1=0.636) — **ไม่ใช่ extraction แย่จริง** ดู caveat ด้านล่าง

## รายฟิลด์ (รหัสวิชา / ชื่อไทย ⭐ / ชื่ออังกฤษ ⭐)

| run | code exact | name_th CER | name_th WER | name_th exact | name_en exact |
|---|---|---|---|---|---|
| ait | 0.756 | 0.277 | 0.270 | 0.732 | 0.000 |
| bit_no_coop | 0.884 | 0.121 | 0.120 | 0.884 | 0.000 |
| bit_coop | 0.810 | 0.245 | 0.277 | 0.810 | 0.000 |
| ours_coop | 0.778 | 0.396 | 0.409 | 0.711 | 0.000 |
| dsba_no_coop | 0.717 | 0.441 | 0.454 | 0.696 | 0.000 |
| it_no_coop | 0.722 | 0.300 | 0.343 | 0.667 | 0.000 |
| it_coop | 0.736 | 0.295 | 0.330 | 0.679 | 0.000 |

**`name_en exact = 0.000` ทุก run โดยไม่มีข้อยกเว้น** — qwen3:4b ไม่ยอม emit ชื่อวิชาภาษาอังกฤษออกมา
เป็น known limitation ที่บันทึกไว้ตั้งแต่ 2026-09-10 (session แรกสุด) ไม่ใช่เรื่องใหม่

**อัปเดต 2026-09-16 (ต่อ):** คอลัมน์ `name_th CER/WER/exact` ของ `ours_coop`, `dsba_no_coop`,
`it_coop`, `it_no_coop` รีเฟรชใหม่แล้ว หลังแก้ typo สะกดผิดใน `data/ground_truth/*.json` 7 จุด (ดู
`Lab8b_ocr_system/PROGRESS.md` หัวข้อ "แก้ typo ใน ground truth ที่ยืนยันแล้ว") — `name_th exact`
ดีขึ้นชัดเจน (ours_coop 0.600→0.711, dsba_no_coop 0.587→0.696, it_coop 0.642→0.679,
it_no_coop 0.630→0.667) เพราะ PRED สะกดถูกอยู่แล้วตั้งแต่ต้น แค่ GT เคยสะกดผิดทำให้ mismatch เทียม
**ตาราง "สรุปรวม" ด้านบน (matched/missed/spurious/P/R/F1) ไม่เปลี่ยนเลยสักตัว** เพราะการจับคู่วิชา
(course matching) ใช้รหัสวิชาเป็น key ไม่ใช่ชื่อ — typo ใน name_th ไม่กระทบการนับว่าวิชาไหน "เจอ"
หรือ "ตก" เลย มีผลแค่กับ field-level CER/WER/exact ของ name_th เท่านั้น

## Caveat สำคัญ — อ่านก่อนเอาตัวเลขไปใช้จริงจัง

`ground_truth_scoped/*.json` กรองด้วย **field `year` ในตัว GT เอง** (`year >= 1`) เท่านั้น
**ไม่ได้ตรวจว่าแต่ละแถวตรงกับหน้าที่ crop จริงทีละหน้า** แบบที่ทำตอนสร้าง `gold_questions.json`
(ที่ยืนยันด้วยตากับหน้าเอกสารจริง)

ยืนยันแล้วว่ากระทบตัวเลขจริง — `ours_coop` (DSBA-coop) ได้ P ต่ำสุด (0.538) เพราะ
`spurious_codes` ส่วนใหญ่คือ `06026243`-`06026256` (วิชา catalog กลุ่มเฉพาะด้าน/เลือกที่ Lab7B
ดึง**ถูกต้อง**จากหน้าที่ crop มาจริง) แต่ GT ให้ `year=0` กับวิชากลุ่มนี้ (เป็น pool ตัวเลือก ไม่ผูก
ภาคตายตัว) เลยถูกกรองทิ้งจาก scoped GT ไปด้วย — ระบบนับว่า "แต่งเกิน" ทั้งที่จริงๆ ถูก

**สรุป:** ตัวเลข P/R ของ DSBA (ทั้ง 2 แผน) ต่ำกว่าความเป็นจริง เพราะ year-filter หยาบเกินไปสำหรับ
หลักสูตรที่มี catalog เยอะในหน้าที่ crop มา ไม่ใช่เพราะสกัดข้อมูลแย่จริง — ถ้าต้องใช้ตัวเลขนี้อย่าง
จริงจัง (เช่น ใส่รายงานส่งอาจารย์) ควรตรวจ `ground_truth_scoped/*.json` เทียบกับ page range จริง
ทีละหลักสูตรก่อน (ผู้ใช้ตัดสินใจเก็บไว้ทำทีหลังถ้าจำเป็น ยังไม่ทำตอนนี้)

## ทำไม course/plan_item ของบาง run ต่ำกว่า baseline เดิม (แค่ VLM non-determinism ไม่ใช่บั๊ก)

รันนี้เป็น **Lab7B ใหม่ทั้งหมด** (ไม่ skip) — VLM เรียกใหม่ทุกครั้งให้ผลไม่เหมือนเดิม 100% เป๊ะ:
- **AIT**: `90641008` (พื้นฐานทักษะการสื่อสารภาษาอังกฤษ) หายทั้งวิชา — วิชาเดิมที่โดนวอเตอร์มาร์กทับ
  เคยแก้ด้วยแพตช์ครั้งเดียว (`merge_page19_patch.py`) ไม่ใช่โค้ดถาวร รอบนี้ไม่มีแพตช์ติดมาด้วย
  known limitation, non-deterministic, **ไม่ patch** ตามนโยบายเดิม
- **DSBA ทั้งสองแผน**: `verify.json` ไม่มี CHK ใหม่ที่ไม่เคยเจอ เป็นความผันผวนปกติ ไม่ใช่ regression
  เชิงโครงสร้าง

## ตรวจ scope ของ ground truth กับเอกสารจริง (2026-09-20)

ตรวจรหัสวิชาจริง (8 หลัก) ทุกตัวที่ถูกนับเป็น "ตก" (missed) หรือ "แต่งเกิน" (spurious) ว่าเป็นความผิดของ
การสกัด หรือความไม่ตรงของ GT/scope — **วิธีตรวจ:** เทียบผล OCR ระดับ run (`intermediate_vlm.md`) + ค้นรหัสในผล OCR
ทั้งเล่มของแต่ละหลักสูตร (`outputs/<ait|bit|dsba|it>/*_curriculum_ocr.txt`) แล้ว**เปิดภาพหน้าจริงยืนยันทุกรหัสจริง**
ที่อยู่ในรายการตก/เกิน (ดูรายการหน้าที่เปิดท้ายหัวข้อ) รหัส wildcard (`9064xxxx`, `06026xxx`, `xxxxxxxx` ฯลฯ) ไม่นับ เพราะเป็นข้อจำกัดที่ทราบอยู่แล้ว (ดู caveat ด้านบน)

### ผลแยกเป็น 4 กลุ่ม

| กลุ่ม | รหัส / run | สรุป |
|---|---|---|
| **A. GT มีวิชาที่ไม่อยู่ในเอกสารหลักสูตรนั้น** | `06016401` ใน AIT, DSBA coop, DSBA no-coop | เป็นวิชาปัจจุบันของหลักสูตร IT (พบในเล่ม IT) แต่ไม่พบเลยทั้งเล่ม AIT และ DSBA และไม่อยู่ในหน้าแผนการศึกษา (ยืนยันด้วยภาพ AIT/DSBA ปี 1 ภาค 1) คาดว่า GT ใส่ผิดหลักสูตร (ที่มาจริงยืนยันไม่ได้ ต้องถามเจ้าของ GT) — **ไม่ใช่ความผิดของระบบสกัด** |
| **B. Scope ไม่ตรง (input มีหน้านอกแผน)** | DSBA coop: `06026243`–`06026256` (14 วิชา) | หน้า `DSBA_19` ที่ใส่เป็น input คือหน้า**แคตตาล็อกวิชาเลือก** (เมนูตัวเลือก ไม่ใช่ช่องในแผนการศึกษา — ยืนยันด้วยภาพ) GT ให้ `year=0` ถูกต้องแล้ว จึงถูกกรองทิ้งด้วย `year>=1` → ระบบสกัดได้ถูกแต่ถูกนับเป็น "แต่งเกิน" ทั้ง 14 ตัว เป็นเรื่องขอบเขต input ไม่ใช่ GT/ระบบผิด |
| **C. OCR ทำวิชาหาย** | AIT `90641008`, `90641004` (ยืนยันด้วยภาพ: อยู่ในตารางปี 1 แต่ทับตราน้ำ) · IT coop/no-coop `06016419`, `06016420` (ปี 2/2 ยืนยันด้วยภาพ: อยู่ในแถวของตัวเอง มีรหัสครบ แต่ OCR ทิ้งรหัสสองตัวนี้) | ความผิดของการสกัดจริง (watermark / OCR ตกหล่น) |
| **D. สกัดได้แต่รวมเซลล์/ใส่ผิดแถว** | IT `06016414/15`, `06016424/25` (ภาพ: แต่ละรหัสเป็นแถวแยกใต้หัวกลุ่มวิชา แต่ OCR รวมเป็นเซลล์ "06016414, 06016415") · IT `90642033`/`90644042` (ภาพ: สองแถวแยกกัน แต่ OCR ใส่ `rowspan=2` ให้รหัสแรกจนรหัส `90644042` หาย ชื่อสองวิชาใช้รหัสเดียวกัน) · BIT no-coop `06036146`+`96642033` (ภาพ: สองแถวแยกกัน OCR รวมเป็นค่าเดียว) · DSBA no-coop `06066100`, `90643021` (ภาพ ปี 3/2: เป็นแถวปกติที่อยู่ต่อจากแถววิชาเลือกกลุ่ม 4 แต่ระบบเอาไปใส่ชื่อ "วิชาเลือกกลุ่ม… 4" และ year=0) · IT `06016418` (ภาพ: ปรากฏจริง 2 ครั้งในสองแทร็กของปี 3/1 GT จึงมี 2 รายการ) | ความผิดของการสกัด/การแปลงข้อมูล ตรงกับข้อจำกัด rowspan ที่บันทึกไว้ใน `PROGRESS.md` |
| **E. คู่ "หรือ" — จับคู่คนละรูปแบบ** | AIT `06046443 หรือ 06046444`, BIT coop `06036147 หรือ 06036148`, IT coop `06016481 หรือ 06016482` | ยืนยันด้วยภาพ: เป็นแถว "A หรือ B" จริง (สหกิจศึกษา/ต่างประเทศ) ระบบแยกเป็นสองรหัส GT เก็บเป็นค่าเดียว จึงนับทั้ง "ตก" และ "เกิน" — ไม่ใช่ความผิดของทั้ง GT และการสกัด แต่เป็นความต่างของรูปแบบการเก็บ |

### ตัวเลขถ้าตัดกลุ่ม A และ B ออก (คำนวณด้วย `evaluate()` เดิม ไม่ได้แก้ไฟล์ GT/รายงานหลัก)

| run | ตัดอะไร | GT | matched | spurious | P | R | F1 (เดิม → ปรับ) |
|---|---|---|---|---|---|---|---|
| ait | A (`06016401`) | 41→40 | 31 | 6 | 0.838 | 0.756→0.775 | 0.795 → 0.805 |
| dsba_coop | A | 45→44 | 35 | 30 | 0.538 | 0.778→0.795 | 0.636 → 0.642 |
| dsba_coop | A + B (ตัด pred 14 ตัวออก) | 44 | 35 | 30→16 | 0.538→0.686 | 0.795 | 0.636 → **0.737** |
| dsba_no_coop | A | 46→45 | 33 | 19 | 0.635 | 0.717→0.733 | 0.673 → 0.680 |

หมายเหตุการอ่านผล:
- ตัวเลขในตาราง "สรุปรวม" ด้านบนยังเป็นค่าเดิมทั้งหมด **ไม่ได้แก้ GT** ตัวเลขที่ปรับข้างบนมีไว้ให้เห็นขนาดของผลกระทบเท่านั้น
- `ours_coop` (DSBA coop) ที่ดูแย่ที่สุด (F1=0.636) เป็นผลจากกลุ่ม B (scope) **ส่วนหนึ่ง** — 14 จาก 30 ตัวที่นับว่าเกิน
  ตัดออกแล้ว F1 ขึ้นเป็น 0.737 แต่ spurious ที่เหลือ 16 ตัวส่วนมากคือ wildcard และกลุ่ม D จึงยังไม่ใช่ทั้งหมด
  (ปรับข้อความ "ไม่ใช่ extraction แย่จริง" ใน caveat ด้านบนให้อ่านตามตรงนี้)
- หน้าที่เปิดดูภาพยืนยัน: AIT หน้า 23, 26 · BIT no-coop หน้า 29 · BIT coop หน้า 34, 35 · DSBA coop `DSBA_19`, `DSBA_28`
  · DSBA no-coop หน้า 23, 28 · IT coop หน้า 40, 41, 43, 44 · IT no-coop หน้า 34, 35, 36 — รวมทุกรหัสจริงที่อยู่ใน
  รายการตก/เกินแล้ว ยกเว้นรหัส wildcard (`xxxx`) ที่ไม่นับ ส่วนหน้าอื่นของแผนที่ไม่มีรหัสในรายการตก/เกินไม่ได้เปิดดู
- **สรุป:** ไม่พบว่า GT ผิดนอกจาก `06016401` (กลุ่ม A) ส่วนที่เหลือคือความผิดของการสกัด (C, D) ขอบเขต input (B) และ
  ความต่างรูปแบบคู่ "หรือ" (E) — ตัวเลขปรับด้านบนจึงตัดเฉพาะ A และ B เพราะสองกลุ่มนี้ไม่ใช่ความผิดของการสกัด
