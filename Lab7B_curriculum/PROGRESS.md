# Lab 7B — Progress Log

หมายเหตุ: ไฟล์นี้ append เท่านั้น ห้ามลบ/แก้ประวัติเก่า

---

## 2026-09-09 — แก้ไขช่วงหน้า (สำคัญ)

ใบงานตัวอย่างใช้ `--pages "42-58"` แต่ไม่ตรงกับ `data/DSBA_curriculum.pdf` จริง
ตรวจสอบด้วยการอ่าน PDF โดยตรงแล้วพบว่า:
- หน้า 30 (เลขพิมพ์ในเล่ม "29" ท้ายหน้า) เริ่ม "3.1.4.2 แผนการศึกษาที่เข้าโครงการสหกิจศึกษา" + ปีที่ 1 ภาคที่ 1
- หน้า 36 จบด้วยปีที่ 4 ภาคที่ 2 (06026259/06026260) + "รวมตลอดหลักสูตร 132 หน่วยกิต"
- หน้า 37 เป็นประวัติอาจารย์ (3.2) ไม่เกี่ยวกับตารางแผนการศึกษา

**ตัดสินใจ: ใช้ `--pages "30-36"` ในทุกคำสั่งของแล็บนี้แทน 42-58**

ผลลัพธ์เดิมที่เคยรันด้วย `--pages 42-58` (โฟลเดอร์ `output/`, `output_full/`) ถูกย้ายไปเก็บที่
`output_STALE_pages42-58/` และ `output_full_STALE_pages42-58/` (ไม่ลบทิ้ง เผื่อใช้อ้างอิง) — ถือว่าใช้ไม่ได้ ต้องรันใหม่ทั้งหมด

---

## 2026-09-09 — ข้อ 1: ตรวจ environment (--check)

คำสั่งที่รันจริง:
```
cd groupB_curriculum
source ../.venv/Scripts/activate
PYTHONIOENCODING=utf-8 python lab7b_curriculum.py --check
```

ผลลัพธ์: ผ่านครบทุกข้อ (ollama 0.33.3, scb10x/typhoon-ocr1.5-3b, qwen3:4b, fitz/PIL/requests/pdfplumber/pythainlp, tesseract)

ปัญหาที่เจอ:
- รัน `python3` ตรงๆ ไม่เจอ (ไม่มี python3 ใน PATH บน Windows) ต้องใช้ `python` แทน
- ค่า default ของ `python` (Python 3.14, cp1252 console) ทำให้ print ข้อความไทยใน `--check` พัง (`UnicodeEncodeError`)
  → แก้ด้วย `PYTHONIOENCODING=utf-8` ก่อนรันทุกครั้ง (ไม่แก้โค้ดสคริปต์)

สิ่งที่เหลือ: ไม่มี — ข้อ 1 เสร็จสมบูรณ์

---

## 2026-09-09 — ข้อ 2: ไฟล์เล่มหลักสูตรใน data/

คำสั่งที่รันจริง:
```
cp ocr_system/data/input/dsba_curriculum.pdf groupB_curriculum/data/DSBA_curriculum.pdf
```

ผลลัพธ์: ก็อปไฟล์สำเร็จ (54,264,534 bytes) ตรงกับ path ที่คำสั่งตัวอย่างในใบงานส่วนที่ 4 ใช้ (`data/DSBA_curriculum.pdf`)

สิ่งที่เหลือ: ไม่มี

---

## 2026-09-09 — ข้อ 3: Round 1 benchmark (ส่วน 6.1) — หน้า 30-32

คำสั่งที่รันจริง:
```
cd groupB_curriculum
source ../.venv/Scripts/activate
PYTHONIOENCODING=utf-8 python lab7b_curriculum.py \
  --input data/DSBA_curriculum.pdf \
  --pages "30-32" \
  --gt gt/DSBA_academic_plan_coop.json \
  --pipeline all \
  --out out_compare/
```

ใช้เวลาจริง: ~70 นาที (ช้ากว่าใบงานประมาณไว้ที่ 10 นาที มาก — ดูเหตุผลด้านล่าง)

### ผลลัพธ์ที่ได้ (out_compare/comparison.csv)

**baseline (Tesseract):** สกัดได้ **0 วิชา** — `_rule_based_parse` แกะ regex ไม่เจอเลยแม้ Tesseract จะอ่านตัวอักษรได้
(568/619/506 ตัวอักษรต่อหน้า) เพราะ regex คาดหวังรูปแบบบรรทัดเดียว `<รหัส8หลัก> <ชื่อ> <N(N-N-N)>`
แต่ตารางจริงมีชื่อวิชาไทย/อังกฤษคนละบรรทัดในเซลล์เดียวกัน ทำให้ regex จับไม่ติดสักแถว
→ `run_pipeline` เห็นว่า `courses` ว่าง จึงไม่เขียน `pred_baseline.json` และไม่มีแถว baseline ใน comparison.csv เลย
(นี่คือพฤติกรรมที่ตั้งใจของสคริปต์ ไม่ใช่บั๊ก — บันทึกไว้เป็นผล baseline ที่แย่ที่สุดเท่าที่จะแย่ได้)

**text vs vlm (ตัวเลขจาก comparison.csv, N=90 ต่อ attribute เทียบกับ GT ทั้งชุด 90 วิชา):**

| attribute | text CER | vlm CER | text exact% | vlm exact% |
|---|---|---|---|---|
| รหัสวิชา | 0.847 | 0.923 | 15.6% | 7.8% |
| ชื่อวิชา(ไทย) | 0.911 | 0.948 | 11.1% | 5.6% |
| ชื่อวิชา(อังกฤษ) | 1.000 | 1.000 | 0% | 0% |
| หน่วยกิต | 0.865 | 0.933 | 15.6% | 7.8% |
| ปี/ภาค | 0.844 | 0.922 | 15.6% | 7.8% |
| หมวดวิชา | 1.000 | 1.000 | 0% | 0% |
| บังคับ/เลือก | 1.000 | 1.000 | 0% | 0% |
| วิชาบังคับก่อน | 0.110 (n=14) | 0.000 (n=7) | 92.9% | 100% |

text จับคู่วิชาได้ 14/90, vlm จับคู่ได้ 7/90 (ทั้งคู่ต่ำเพราะ GT คือทั้ง 90 วิชาตลอดหลักสูตร
แต่หน้า 30-32 มีแค่ปีที่ 1 ภาค 1 และบางส่วนของภาค 2 เท่านั้น — recall ต่ำเป็นเรื่องคาดหมายได้ ไม่ใช่ตัวชี้วัดคุณภาพหลักของรอบนี้)

ตรวจสอบชนิด PDF ด้วย pdfplumber (หน้า 30-36): ได้ 676–1,032 ตัวอักษร/หน้า (>500) → **เป็น digital PDF** ยืนยันว่า pipeline text ใช้ได้และควรเป็นตัวเลือกหลัก

**สรุปผู้ชนะ: pipeline `text`**
เหตุผล: CER ต่ำกว่า vlm ในทุก attribute ที่มีความแตกต่าง (รหัสวิชา, ชื่อวิชาไทย, หน่วยกิต, ปี/ภาค) และจับคู่วิชาได้มากกว่า (14 vs 7)
เข้ากับที่ pdfplumber ยืนยันว่าเป็น digital PDF (ไม่ต้องพึ่ง OCR เลย)

**หมายเหตุเรื่องเวลา (ผิดจากที่คาด):** จาก `_meta.elapsed_sec` ใน pred_text.json/pred_vlm.json: text = 2,884.4 วิ (~48 นาที),
vlm = 1,297.5 วิ (~21.6 นาที) — vlm เร็วกว่า text ในรอบนี้ ซึ่งสวนทางกับที่ใบงานบอกว่า text ควรเร็วกว่า vlm ~50 เท่า
สาเหตุที่น่าจะเป็น: เครื่องนี้ไม่มี GPU ใช้งานจริง (`ollama ps` แสดง qwen3:4b ที่ 6%GPU/94%CPU) การรันบน CPU ล้วนที่ context 32768
ทำให้ "ต้นทุนโหลดโมเดล" (model load/unload ระหว่างสลับโมเดล) ครอบงำเวลารวมมากกว่าจำนวนขั้นตอนจริง —
pipeline text เป็นการเรียก qwen3:4b เย็น (cold) เป็นครั้งแรกของการรันทั้งหมด ส่วน vlm ได้เรียก qwen3:4b ต่อจาก text
จึงอาจยังมี weight ค้างอยู่ในหน่วยความจำ/แคชของระบบ ทำให้ jsonify ของ vlm เร็วกว่า
**สรุป: เลือก text เป็นผู้ชนะจากคุณภาพ (CER/recall) ไม่ใช่จากเวลา** — เวลาที่วัดได้ในรอบนี้มีปัจจัยกวนจากลำดับการรันแบบ `--pipeline all` ในเครื่องเดียว

ปัญหาที่เจอ:
- baseline ได้ 0 วิชาเสมอ (regex ธรรมดาไม่รองรับ multi-line cell) — ตามคาดตามแนวคิดของแล็บ (baseline ควรแพ้)
- หมวดวิชา/บังคับ-เลือก ได้ CER=1.0 ทั้งสอง pipeline เพราะ LLM คืนค่า `None` ทุกวิชาที่จับคู่ได้ในรอบนี้ (ตรวจจาก evaluation detail: PRED='None' ทุกแถว) — จะต้องดูอีกทีตอนรันเต็มหน้า 30-36 ว่าเป็นปัญหาถาวรหรือแค่ตัวอย่างย่อยไม่มีบริบทพอ
- เวลารันจริงช้ากว่าที่ใบงานประมาณไว้มาก (เครื่องนี้ไม่มี GPU ใช้งานได้เต็มที่)

สิ่งที่ยังเหลือจาก list: ข้อ 4-8

---

## 2026-09-09 — ข้อ 5: ตรวจ intermediate_vlm.md (ส่วน 6.3)

ตรวจ `out_compare/intermediate_vlm.md` (ผลจาก Typhoon-OCR อ่านหน้า 30-32 เป็น Markdown/LaTeX) ทีละหน้า
(หน้าแยกด้วย `---`):

- **หน้า 1 (PDF หน้า 30, ปีที่ 1 ภาคที่ 1): อ่านตารางถูกต้องสมบูรณ์**
  Typhoon-OCR ห่อผลลัพธ์เป็นเอกสาร LaTeX (`\documentclass{article}...`) มี boilerplate ของ tikz-cd
  ที่ไม่เกี่ยวข้องปนมาด้วย (model แต่งขึ้นเอง ไม่ได้อยู่ในภาพจริง) แต่ **เนื้อหาตารางในแท็ก `<table><tr><td>` ถูกต้อง 100%**
  ทั้ง 7 วิชา (06026200 ... 90644007), ชื่อไทย/อังกฤษ, หน่วยกิตรูปแบบ N(N-N-N), และแถวรวม "18" ตรงกับ GT ทุกตัวอักษร

- **หน้า 2 และหน้า 3 (PDF หน้า 31-32, ปีที่ 1 ภาคที่ 2 เป็นต้นไป): ล้มเหลวสมบูรณ์ — ไม่มีเนื้อหาตารางเลย**
  Typhoon-OCR หลุดเข้าสู่ "repetition loop": วนพิมพ์ `\tikzset{...fill=yellow/green/blue/red...}` ซ้ำเดิมเป็นร้อยรอบ
  จนกินพื้นที่เกือบทั้งไฟล์ ไม่มีแท็ก `<table>` หรือรหัสวิชาใดๆ ปรากฏเลยแม้แต่ตัวเดียว
  ไฟล์ที่หน้า 3 ถูกตัดกลางคำ (`\t` ที่บรรทัดสุดท้าย) ตรงกับสัญญาณเตือนแบบที่ 1 ในใบงาน (6.4) คือ "ผลลัพธ์อาจถูกตัดเพราะชน num_predict"

**สรุปตามหลัก 6.3:** ปัญหาอยู่ที่ "ขั้นอ่านภาพ" (Markdown ผิดตั้งแต่ต้น) ไม่ใช่ขั้นจัด JSON —
เพราะแม้ Markdown จะผิด (วนซ้ำ) แต่ JSON stage (qwen3:4b) ก็ทำงานถูกต้องคือคืนค่าว่างแทนที่จะแต่งข้อมูลเท็จขึ้นมา
(ไม่พบ hallucination รหัสวิชาใหม่ในผลลัพธ์สุดท้าย) — สาเหตุนี้เองคือที่มาของ "จับคู่วิชา: เจอ 7/90" ในผลลัพธ์ Round 1
(7 วิชาทั้งหมดมาจากหน้า 1 หน้าเดียว หน้า 2-3 ไม่ได้ส่งข้อมูลเข้าสู่ pipeline เลยสักวิชา)

นี่คือหลักฐานเชิงประจักษ์อีกชิ้นที่สนับสนุนว่า pipeline `text` เหมาะกับเอกสารนี้มากกว่า `vlm`:
เอกสารเป็น digital PDF ที่ pdfplumber ดึงข้อความได้แม่นยำ 100% อยู่แล้ว ไม่จำเป็นต้องเสี่ยงกับความไม่เสถียรของ VLM ที่อาจ
"หลุด" เป็น repetition loop แบบสุ่มเมื่อภาพซับซ้อน (ตารางเส้นกรอบเยอะ) โดยไม่มีสัญญาณเตือนล่วงหน้าจากภาพ

สิ่งที่ยังเหลือจาก list: ข้อ 4, 6-8

---

## 2026-09-10 — ข้อ 4: Round 2 (ส่วน 6.2) — ปัญหา background task ถูก kill + วิธีแก้

**ปัญหา:** รัน Round 2 (`--pipeline text --pages "30-36" --out out_full/`) แบบ background 2 ครั้งแรก
ถูก "killed" ทั้งคู่เกือบจะทันทีหลังเริ่ม (ก่อนแม้แต่ print บรรทัด "แปลงเป็นภาพแล้ว") ทั้งที่:
- ทดสอบแยกเฉพาะ `load_pages()` (แปลง PDF→ภาพ 7 หน้า) แบบ foreground ทำงานเสร็จเร็วปกติ ไม่มีปัญหา
- ทดสอบ background loop เปล่าๆ (sleep 10s ×30 = 5 นาที) ทำงานจนจบครบไม่ถูก kill

สรุปสาเหตุที่เป็นไปได้มากที่สุด: คำสั่ง background ที่ใช้ output redirect ไปไฟล์ (`> log 2>&1`) ผ่าน tool
รันเป็น "tracked background task" ที่ดูเหมือนจะถูก external process/timeout cap ตัดก่อนที่งานจริง (ซึ่งกินเวลาระดับ
ชั่วโมงเพราะ CPU-only inference) จะเสร็จ — ไม่ใช่บั๊กของสคริปต์ lab7b_curriculum.py เอง

**วิธีแก้ที่ใช้:** รันคำสั่งจริงแบบ fully detached ด้วย `nohup ... & disown` แยกออกจาก process tree ของ tool
แล้วใช้ Monitor แยกต่างหาก (persistent) grep บรรทัดสำคัญจาก log แทนการรอ background-task tracked ปกติ
วิธีนี้ทำให้กระบวนการจริงรันต่อได้ไม่จำกัดเวลาโดยไม่ถูก kill

คำสั่งที่รันจริง (ตัวที่สำเร็จ):
```
cd groupB_curriculum
source ../.venv/Scripts/activate
nohup env PYTHONIOENCODING=utf-8 PYTHONUNBUFFERED=1 python -u lab7b_curriculum.py \
  --input data/DSBA_curriculum.pdf --pages "30-36" \
  --gt gt/DSBA_academic_plan_coop.json \
  --pipeline text --out out_full/ > out_full_run.log 2>&1 &
disown
```

**ผลระหว่างรัน:** ก้อนที่ 1-4/7 สำเร็จ (7, 7, 6, 7 วิชาตามลำดับ)
ก้อนที่ 5/7 (หน้า 34 ของเล่ม = ปีที่ 3 ภาคที่ 2 โดยประมาณ) ล้มเหลว: "เรียก qwen3:4b ไม่สำเร็จ: โมเดลตอบว่าง"
(ตามที่ script ออกแบบไว้ — ก้อนเดียวพังไม่ทำให้ทั้งงานพัง แต่วิชาในหน้านั้นจะหายไปจากผลรวม)
→ วางแผนแพตช์เฉพาะหน้า 34 แยกหลัง Round 2 จบ (รัน `--pages "34" --pipeline text` เดี่ยวๆ แล้ว merge เข้า
pred_text.json ของ out_full/ ด้วยฟังก์ชัน `merge_chunks`/`evaluate` เดิมจากสคริปต์ ไม่แก้ตรรกะใดๆ)

สิ่งที่ยังเหลือ: รอก้อนที่ 6-7 จบ, แพตช์ก้อนที่ 5, แล้วไปข้อ 6-8

---

## 2026-09-10 — Round 2 จบแล้ว แต่ก้อนที่ 5, 6, 7 ล้มเหลวทั้งหมด (ไม่ใช่แค่ก้อนเดียว)

**ผล Round 2 ที่ได้จริง:** `out_full/pred_text.json` มี **27 วิชา** (จากก้อนที่ 1-4 เท่านั้น: 7+7+6+7)
ก้อนที่ 5 (หน้า 34), 6 (หน้า 35), 7 (หน้า 36) ล้มเหลวทั้งหมดด้วยสาเหตุเดียวกันทุกครั้ง (ดู log `out_full_run.log`):

```
(qwen3:4b: 453.1 วิ, 0 ตัวอักษร, 8,192 tokens)
⚠ ผลลัพธ์อาจถูกตัดเพราะชน num_predict --> ลดจำนวนหน้าต่อ chunk หรือเพิ่ม num_predict
❌ ก้อนที่ 5 ล้มเหลว: เรียก qwen3:4b ไม่สำเร็จ: โมเดลตอบว่าง
```

**วินิจฉัย:** โมเดลใช้ token budget เต็ม 8,192 (`num_predict`) ไปกับการ "คิด" ใน `<think>...</think>`
(qwen3 เป็น reasoning model ที่คิดก่อนตอบโดย default) จนไม่เหลือโควตาผลิตคำตอบจริงเลย (`content` ว่างสนิท 0 ตัวอักษร)
เกิดซ้ำ 3/3 ครั้งเป๊ะๆ ในแต่ละก้อน (ทั้ง built-in retry ×2 ของ `ollama_chat` เอง) → **ไม่ใช่ความผิดพลาดชั่วคราว
แต่เป็นปัญหาเชิงโครงสร้างของเนื้อหาหน้า 34-36** (ตารางปีที่ 3 ภาคปลาย ถึง ปีที่ 4 ซึ่งมีวิชาเลือกเสรี/รหัส xxxxxxx
และเงื่อนไข "หรือ" ที่ซับซ้อนกว่าหน้าต้นๆ ทำให้โมเดลคิดยาวขึ้นจนชนเพดาน)
ตรงกับปัญหาที่ใบงานเตือนไว้ในตาราง 3.2 ("output ยาวเกิน num_predict") และคำแนะนำใน 6.4 พอดี

ตรวจสอบแล้วว่า **ไม่ใช่ ollama ล่ม**: ping `qwen3:4b` ตรงๆ หลัง Round 2 จบ ตอบกลับปกติทันที

**ทางแก้ที่เลือกใช้ (อยู่ในกรอบที่อนุญาต — ปรับ parameter ผ่าน env var เท่านั้น ไม่แก้โค้ด):**
`num_predict` เป็นค่าคงที่ในโค้ด (ไม่มี env var ให้ปรับ) และ `LAB7_CHUNK` ก็ลดต่อไม่ได้อีกแล้ว (อยู่ที่ 1 หน้า/ก้อนอยู่แล้ว)
จึงเลือกใช้ตัวแปรที่ 3 จาก 4 หัวข้อในส่วน 6.5 คือ **ขนาดโมเดล**: ลองสลับเป็น `LAB7_MODEL_TEXT=qwen3:1.7b`
กับ 3 หน้าที่ตกไป (34, 35, 36) เป็นรายหน้า แล้ว merge ผลที่ได้กลับเข้า `out_full/pred_text.json`
(ใช้ฟังก์ชัน `merge_chunks`/`evaluate` เดิมของสคริปต์ ไม่แก้ตรรกะ)

โมเดล qwen3:1.7b ยังไม่มีในเครื่อง (`ollama list` มีแค่ qwen3:4b + typhoon-ocr1.5-3b) → กำลัง `ollama pull qwen3:1.7b`
(สอดคล้องกับใบงานส่วน 2.3 ที่แนะนำโมเดลนี้สำหรับเครื่องแรมน้อยอยู่แล้ว)

สิ่งที่ยังเหลือ: รอ pull เสร็จ, รันหน้า 34/35/36 ด้วย qwen3:1.7b, merge เข้า out_full/pred_text.json,
คำนวณ evaluation.json/comparison.csv ใหม่, บันทึกผลว่าโมเดลเล็กช่วยแก้ปัญหานี้ได้จริงหรือไม่ (ใช้เป็นผลข้อ 6 ด้วย)

---

## 2026-09-10 — ข้อ 4/10 เสร็จสมบูรณ์: แพตช์หน้า 34-36 ด้วย qwen3:1.7b และ merge ผลสำเร็จ

**ผลการแพตช์ (คำสั่ง: `LAB7_MODEL_TEXT=qwen3:1.7b python lab7b_curriculum.py --input data/DSBA_curriculum.pdf --pages "34" --pipeline text --out out_patch_page34/` ทำซ้ำสำหรับหน้า 35, 36):**

| หน้า | วิชาที่ได้ | เวลา |
|---|---|---|
| 34 | 10 | 34.5 วิ |
| 35 | 10 | 36.0 วิ |
| 36 | 6  | 32.9 วิ |

qwen3:1.7b **สำเร็จทั้ง 3 หน้าในความพยายามครั้งแรก** ไม่มี "โมเดลตอบว่าง" เลยแม้แต่ครั้งเดียว
และเร็วกว่า qwen3:4b อย่างมาก (~30-36 วิ/หน้า เทียบกับ qwen3:4b ที่ใช้ ~400-460 วิ/ครั้งต่อการเรียก 1 ครั้ง แม้จะสำเร็จ)

**สาเหตุที่แท้จริงของปัญหา qwen3:4b (แก้ไขข้อสรุปจากรายการก่อนหน้า):**
ตรวจ log `out_full_run.log` แบบละเอียดทุกบรรทัด (ไม่ใช่แค่สรุปท้ายก้อน) พบว่า **ทุกก้อน แม้แต่ก้อนที่สำเร็จ
ก็เจอ "โมเดลตอบว่าง" (0 ตัวอักษร, เต็ม 8,192 tokens) มาก่อนอย่างน้อย 1 ครั้งเสมอ** แล้วสำเร็จในการลองใหม่ครั้งถัดไป:
- ก้อนที่ 1: ล้มเหลว 1 ครั้ง → สำเร็จ (7 วิชา)
- ก้อนที่ 2: สำเร็จทันที (7 วิชา) — เคสเดียวที่ไม่มีล้มเหลวก่อน
- ก้อนที่ 3: ล้มเหลว 1 ครั้ง → สำเร็จ (6 วิชา)
- ก้อนที่ 4: ล้มเหลว 1 ครั้ง → สำเร็จ (7 วิชา)
- ก้อนที่ 5, 6, 7: ล้มเหลวทั้ง 3 ครั้ง (เต็มโควตา retry) → ไม่มีข้อมูล

สรุปแก้ไข: **นี่ไม่ใช่ปัญหาเฉพาะหน้าที่ซับซ้อน (34-36) แต่เป็นพฤติกรรม flaky ของ qwen3:4b ที่เกิดสูงถึง ~60%
ต่อการเรียกหนึ่งครั้ง** (โมเดล "คิด" ใน `<think>` ยาวจนกินโควตา `num_predict=8192` หมดก่อนได้ตอบจริง) —
หน้า 34-36 แค่โชคร้ายที่ล้มเหลวครบ 3/3 ครั้งติดกันเท่านั้น ไม่ใช่เพราะเนื้อหายากกว่าโครงสร้าง (แม้เนื้อหาหน้าท้ายๆ
จะมีความซับซ้อนกว่าจริงในแง่รหัสวิชาเลือกแบบ xxxxxxx และเงื่อนไข "หรือ" ก็อาจมีส่วนเสริมให้ยิ่งคิดนานขึ้นก็เป็นได้
แต่หลักฐานที่มีไม่พอจะฟันธงว่าเป็นสาเหตุหลัก)

**การ merge:** เขียนสคริปต์ผู้ช่วยเรียกใช้ฟังก์ชันเดิมของแล็บ (`merge_chunks`, `evaluate`, `verify_internal`,
`lab7_metrics.save_csv/print_table/stats_to_dict`) โดยไม่แก้ตรรกะใดๆ เพื่อรวมผล 4 ก้อนจาก qwen3:4b (out_full/) +
3 หน้าที่แพตช์จาก qwen3:1.7b เข้าด้วยกัน แล้วคำนวณ evaluation.json/comparison.csv ใหม่ทับที่ `out_full/`

**ผลลัพธ์สุดท้ายของ Round 2 (out_full/, รวมข้อมูลครบทั้ง 7 หน้า 30-36):**
- รวม 53 วิชาดิบ → กรองซ้ำออก 1 รายการ → **52 วิชา**
- จับคู่กับ GT (90 วิชา, ตัดแถวไม่ใช่รายวิชาออก 1 แถว → 89): **เจอ 26 | ตก 64 | แต่งเกิน 26 → P=0.500 R=0.289 F1=0.366**
- CER รวม (micro) = 0.872, (macro) = 0.677 — สูงกว่าที่คาดสำหรับ digital PDF เพราะปัญหาการจับคู่ (ดูด้านล่าง)

**ทำไม recall ต่ำ (26/90) ทั้งที่ครอบคลุมทั้งตารางแล้ว:** ไม่ใช่เพราะสกัดวิชาไม่ครบ (ได้ 52 วิชาจาก ~53-60
แถวจริงในตารางซึ่งใกล้เคียงสมเหตุสมผล) แต่เพราะ**รหัสวิชาเลือกที่เป็น placeholder ซ้ำกัน** (เช่น "xxxxxxx" ใช้ซ้ำ
สำหรับ "วิชาเลือกเสรี 1" และ "วิชาเลือกเสรี 2" ที่เป็นคนละแถวใน GT แต่รหัสเหมือนกัน) ทำให้ตัวจับคู่แบบ align-by-code
จับคู่ผิดคู่/นับซ้ำซ้อนได้ทั้ง "ตก" และ "แต่งเกิน" พร้อมกัน — ตรงกับปัญหาที่เคย commit ไว้ในโปรเจกต์นี้ก่อนหน้า
(README: "why GE-elective placeholders aren't matched against ground_truth") เป็นข้อจำกัดที่รู้อยู่แล้วของวิธีเทียบผล
ไม่ใช่บั๊กใหม่ที่ต้องแก้

**ผล verify_internal (ใช้เป็นคำตอบข้อ 7 ไปในตัว) — พบปัญหาจริง 3 ใน 5 กฎ:**
```
verify_internal ok=False n_courses=52 total_credits=42
  - 90644xxx: ปี/ภาค = 0 แต่ไม่ได้ระบุ flexible_year_semester        (กฎข้อ 4)
  - 06026215: ระบุปีชัดเจนแล้ว ไม่ควรมี flexible_year_semester        (กฎข้อ 4 กลับด้าน)
  - 06026259: ระบุปีชัดเจนแล้ว ไม่ควรมี flexible_year_semester        (กฎข้อ 4 กลับด้าน)
  - 06026260: ระบุปีชัดเจนแล้ว ไม่ควรมี flexible_year_semester        (กฎข้อ 4 กลับด้าน)
  - รหัสวิชาผิดรูปแบบ: 'xxxxxx' (×2)                                  (กฎข้อ 1 — ต้องยาว 8 ตัวอักษรพอดี)
  - ภาค 1/2 มีแค่ 6 หน่วยกิต — น่าจะอ่านตกวิชา                          (สัญญาณเสริมนอกกฎ 5 ข้อ)
```
วิเคราะห์ที่มา: `'xxxxxx'` (6 ตัว x) เป็นความผิดพลาดของ **qwen3:1.7b** (โมเดลเล็กพิมพ์ placeholder โค้ดสั้นเกินจาก
ต้นฉบับที่เป็น "xxxxxxx" 7 ตัว x + รหัสต้องเป็น 8 หลัก) — เป็น trade-off ที่ต้องรู้ไว้: โมเดลเล็กเร็วกว่าและไม่ล้มเหลว
เพราะ num_predict แต่แม่นยำในการคัดลอกรูปแบบ (format fidelity) ต่ำกว่าเล็กน้อย
ส่วนความขัดแย้งเรื่อง flexible_year_semester (ข้อ 06026215/259/260) เป็นปัญหาที่เกิดจากทั้งสองโมเดล (มาจากคนละก้อน/หน้า)
สะท้อนว่า prompt กติกาข้อ "ปี=0/ภาค=0 ต้องใส่ flexible_year_semester" กับ "ถ้าระบุปีชัดแล้วห้ามใส่" ยังสับสนโมเดลอยู่บ้าง
โดยเฉพาะวิชาแบบ 06026259/06026260 ที่มี "หรือ" (สหกิจในประเทศ/ต่างประเทศ) ซึ่งกำกวมกึ่งหนึ่งระหว่าง "ปีชัดเจน" กับ "ยืดหยุ่น"

สิ่งที่ยังเหลือจาก list: ข้อ 6 (ทดลอง parameter — ใช้ผลข้างต้นเป็นข้อมูล model-size ไปแล้วบางส่วน, กำลังรัน
experiment เพิ่มเพื่อเทียบแบบควบคุมตัวแปรที่ยุติธรรมกว่า), ข้อ 8

---

## 2026-09-10 — ข้อ 6 (การทดลองที่ 1/2): ขนาดโมเดล qwen3:1.7b vs qwen3:4b — พบ hallucination จริง

**การทดลองควบคุม:** รันหน้า 30 เดี่ยวๆ (`--pages "30"`) ด้วย `LAB7_MODEL_TEXT=qwen3:1.7b` เทียบกับผลของ
qwen3:4b บนหน้าเดียวกัน (จากก้อนที่ 1 ของ Round 2 ซึ่งได้ 7 วิชาตรง GT 100%) → `out_expA_qwen17b_page30/`

**ผล:** qwen3:1.7b ใช้เวลา 76.1 วิ (เร็วกว่า qwen3:4b ที่ใช้ ~400+ วิ/ครั้งมาก) และ**สกัด 7 วิชาแรกถูกต้องครบถ้วน**
ตรงกับ GT ทุกตัว **แต่หลังจากนั้นสร้างวิชาซ้ำที่ไม่มีอยู่จริงเพิ่มอีก 6 รายการ** โดยเอาโค้ด/ชื่อวิชาเดิมจาก 4 ใน 7 วิชา
มาแต่ง year=2,3,4 และ **semester=4 ที่ไม่มีอยู่จริงในระบบการศึกษา** (ภาคเรียนมีแค่ 1-2 เท่านั้น) พร้อมใส่
`flexible_year_semester: "3/1, 3/2, 3/3"` ที่เป็นรูปแบบเดา ไม่สอดคล้องกับ schema ที่ควรจะเป็น — ทั้งที่หน้า 30 มีแค่
ตาราง "ปีที่ 1 ภาคที่ 1" เพียงตารางเดียว ไม่มีข้อมูลปี 2-4 อยู่ในหน้านี้เลย

**นี่คือ hallucination ตามนิยามในกติกาข้อ 9 ของใบงานโดยตรง** ("ห้ามสร้างวิชาที่ไม่มีในเอกสาร — ป้องกัน hallucination
ซึ่งเป็นปัญหาที่ตรวจจับยากที่สุด") พบเฉพาะกับ qwen3:1.7b เท่านั้น — qwen3:4b ไม่เคย hallucinate วิชาใหม่ในทุกการทดสอบ
ที่ผ่านมา (ปัญหาของ qwen3:4b คือ "ตอบว่าง" ไปเลยเมื่อไม่มั่นใจ/คิดนานเกิน ซึ่งปลอดภัยกว่าการแต่งข้อมูลเท็จ)

**ตรวจ Thai text fidelity เพิ่มเติม:** qwen3:1.7b มีแนวโน้มตัดวรรณยุกต์/รูปสระท้ายคำ (เช่น "การสร้าง" → "การสราง",
"ข้อมูล" → "ขอมูล", "เรียนรู้" → "เรียนรู") ในหลายวิชาที่สกัดจากหน้า 34-35 (ดู `out_patch_page34/pred_text.json`)
ซึ่งจะกระทบ CER ของ attribute "ชื่อวิชา(ไทย)" โดยตรง แม้เนื้อหาความหมายจะยังพอเข้าใจได้

**ตรวจสอบเพิ่มเติม (สำคัญ): entries ที่หน้า 34-35 ไม่ใช่ hallucination** — เทียบกับ PDF จริงหน้า 34-35 แล้วพบว่า
"06026xxx วิชาเลือกกลุ่มวิทยาการข้อมูล 1 / วิชาเลือกกลุ่มการวิเคราะห์เชิงสถิติ 1 / วิชาเลือกกลุ่มวิศวกรรมข้อมูล 1" ที่ดู
เหมือนซ้ำๆ กันหลายรายการนั้น **มีอยู่จริงในตาราง** — เป็น 1 แถวในตารางจริงที่ระบุ 3 ทางเลือกวิชาเลือกภายใต้รหัสเดียวกัน
"06026xxx" (พร้อม "หรือ" คั่นหน่วยกิต) qwen3:1.7b เลือกแยกแต่ละทางเลือกเป็นคนละ entry ซึ่งเป็นการตีความโครงสร้าง
ที่สมเหตุสมผล (ไม่ใช่แต่งข้อมูลใหม่) แต่ **จัดหมวดผิด**: ใส่ year=0, semester=0 + flexible_year_semester
ทั้งที่แถวเหล่านี้อยู่ใน "ปีที่ 3 ภาคที่ 1" ตายตัว (เป็นการเลือก "วิชาไหน" ไม่ใช่เลือก "เมื่อไหร่") — ควรเป็น year=3,
semester=1 ตามตารางจริง สาเหตุนี้เองที่ทำให้ verify_internal ฟ้อง "06026215 ระบุปีชัดเจนแล้วไม่ควรมี flexible_year_semester"
ก่อนหน้านี้ (คนละวิชาแต่ปัญหาประเภทเดียวกัน: **โมเดลสับสนระหว่าง "เลือกวิชาไหน" กับ "เลือกเรียนเมื่อไหร่"**)

**สรุปข้อ 6 (การทดลองที่ 1 — ขนาดโมเดล):**
| ประเด็น | qwen3:4b | qwen3:1.7b |
|---|---|---|
| ความเร็ว | ช้ามาก (~400-460 วิ/ครั้ง, CPU-only) | เร็วกว่ามาก (~30-75 วิ/ครั้ง) |
| ความล้มเหลวแบบ "ตอบว่าง" (num_predict หมดเพราะคิดนาน) | เกิดบ่อย (~60% ต่อครั้ง) | ไม่เกิดเลยในทุกการทดสอบ |
| Hallucination (แต่งวิชาที่ไม่มีจริง) | ไม่พบเลย | **พบ 1 ครั้งชัดเจน (หน้า 30: แต่ง 6 วิชาปลอม)** |
| ความเที่ยงตรงของอักขระไทย (วรรณยุกต์/สระ) | ปกติดี | มีตกหล่นบางจุด |
| การจัดหมวด year/semester สำหรับแถวที่มีตัวเลือกซ้อน | ยังไม่ทดสอบ | สับสนระหว่าง "เลือกวิชาไหน" กับ "เลือกเมื่อไหร่" |

**บทเรียน:** ไม่มีโมเดลไหน "ดีกว่า" แบบเบ็ดเสร็จ — เป็น trade-off ที่ต้องเลือกตามความเสี่ยงที่ยอมรับได้:
โมเดลใหญ่ (4b) ล้มเหลวแบบ "เงียบ" (คืนค่าว่าง ตรวจจับง่าย สคริปต์ข้ามได้ทันที) ส่วนโมเดลเล็ก (1.7b) เร็วกว่ามาก
แต่ล้มเหลวแบบ "เนียน" (สร้างข้อมูลที่ดูสมเหตุสมผลแต่เป็นเท็จ) ซึ่งตรวจจับยากกว่ามากถ้าไม่เปิดดูไฟล์กลางทาง/เทียบ GT
ทุกครั้ง — ตรงกับหลักการที่ใบงานเน้นย้ำในส่วนที่ 7 (verify_internal ไม่ได้ใช้เฉลยแต่จับความขัดแย้งเชิงโครงสร้างได้)

---

## 2026-09-10 — ข้อ 6 (การทดลองที่ 2/2): ขนาดก้อน LAB7_CHUNK=3 เทียบกับค่า default (1)

**คำสั่งที่รันจริง:**
```
LAB7_CHUNK=3 python lab7b_curriculum.py --input data/DSBA_curriculum.pdf \
  --pages "30-32" --gt gt/DSBA_academic_plan_coop.json --pipeline text --out out_chunk3/
```
(หน้าชุดเดียวกับ Round 1 คือ 30-32 เพื่อเทียบกับผลเดิมที่ chunk=1 ได้ 14 วิชา, CER รหัสวิชา=0.847)

**ผล: ล้มเหลว 100% — ได้ 0 วิชา** โมเดลใช้เต็ม 8,192 tokens ไปกับการคิด (`<think>`) จนไม่เหลือ token
สำหรับคำตอบจริง **ทั้ง 3 ครั้งที่ retry ให้** (612.8, 587.7, 597.4 วิ ตามลำดับ รวมเสียเวลาไป ~30 นาทีได้ 0 วิชา)
เทียบกับตอน chunk=1 (ค่า default) บนหน้าเดียวกันที่ยังพอได้ 14 วิชา (แม้จะเจอ "ตอบว่าง" ในบางครั้งเหมือนกัน
แต่ retry แล้วสำเร็จ เพราะแต่ละครั้งมีแค่ 1 หน้า/ครั้ง เนื้อหาน้อยกว่า)

**สรุปข้อ 6 (การทดลองที่ 2 — ขนาดก้อน):**
| ขนาดก้อน (LAB7_CHUNK) | วิชาที่ได้ (หน้า 30-32) | อัตราสำเร็จของการเรียก LLM |
|---|---|---|
| 1 (default) | 14 | ~40-50% ต่อครั้ง แต่ retry แล้วสำเร็จครบทุกก้อนใน Round 1 |
| 3 | **0** | 0% (ล้มเหลวทั้ง 3 attempt) |

**บทเรียน:** ก้อนเล็กลง (LAB7_CHUNK=1) ไม่ได้ "แลกด้วยการเสีย Recall จากการไม่เห็นภาพรวม" ตามที่ใบงานตั้งคำถามไว้
เท่านั้น — ในกรณีนี้ก้อนเล็กเป็น**เงื่อนไขจำเป็น**ต่อการได้ผลลัพธ์เลยด้วยซ้ำ เพราะโมเดล reasoning (qwen3:4b) ที่ต้อง
"คิด" ก่อนตอบ ยิ่งเนื้อหาต่อคำขอมากขึ้น (3 หน้า/ครั้ง ≈ 16,100 ตัวอักษร) ยิ่งใช้ reasoning tokens มากขึ้นตามไปด้วย
จนชน `num_predict` คงที่ (8,192) แน่นอนกว่าเดิม — ตรงกับคำเตือนในใบงานส่วน 3.2 เป๊ะ ("output ยาวเกิน num_predict")
เพียงแต่ในเคสนี้ตัวการไม่ใช่ output ยาว แต่เป็น**ความคิด**ยาว (thinking overhead ของ reasoning model)
ซึ่งเป็นประเด็นที่ใบงานไม่ได้เจาะจงไว้ตรงๆ แต่ผลกระทบเหมือนกันคือ token budget หมดก่อนได้คำตอบ

**ข้อ 6 เสร็จสมบูรณ์ (ทำ 2/4 หัวข้อตามที่ใบงานกำหนด): ขนาดโมเดล + ขนาดก้อน**
สิ่งที่ยังเหลือจาก list: ข้อ 7 (มีข้อมูล verify_internal จาก Round 2 merged แล้ว จะสรุปแยกอีกครั้ง), ข้อ 8

---

## 2026-09-10 — ข้อ 7: ตรวจความสอดคล้องเชิงโครงสร้าง (verify_internal, 5 กฎ)

ใช้ผล `verify_internal` ที่รันอัตโนมัติจากผลลัพธ์ Round 2 (merged, 52 วิชา, หน้า 30-36 ครบ) เป็นชุดข้อมูลหลัก
เพราะเป็นชุดข้อมูลที่ครอบคลุมทั้งตารางจริงมากที่สุดที่มีอยู่ตอนนี้ (สคริปต์รันฟังก์ชันนี้ให้อัตโนมัติทุกครั้งที่มีผลลัพธ์
จาก pipeline อยู่แล้ว ไม่ต้องเรียกแยก — ดู `main()` บรรทัด 1043-1054 ของ lab7b_curriculum.py)

**ผล: `ok=False`, n_courses=52, รวม 42 หน่วยกิต (เฉพาะภาคที่ระบุปี/ภาคชัดเจน)**

ไล่ตาม 5 กฎ:

| กฎ | เนื้อหา | ผ่าน? | รายละเอียด |
|---|---|---|---|
| 1 | รหัสวิชายาว 8 ตัว เป็นตัวเลขหรือ x | **ไม่ผ่าน** | พบ `'xxxxxx'` (6 ตัว x) ซ้ำ 2 รายการ จากวิชาเลือกเสรีหน้า 36 — ต้นฉบับจริงคือ "xxxxxxx" (7 ตัว x) แต่ต้องครบ 8 หลักตามกฎ ตัวต้นฉบับเองก็สั้นไป 1 ตัวอยู่แล้ว (ผิดตั้งแต่ต้นฉบับ ไม่ใช่ความผิดโมเดลทั้งหมด) ส่วนโมเดล (qwen3:1.7b) คัดลอกมาผิดอีกชั้น (ตัดเหลือ 6 ตัว) |
| 2 | หน่วยกิตต้องอยู่ในรูป N(N-N-N) | **ผ่าน** | ไม่พบ error เรื่องรูปแบบหน่วยกิตใน issues ที่รายงาน |
| 3 | category และ type ต้องอยู่ในชุดที่กำหนด | **ผ่าน** | ไม่พบ error หมวดวิชานอกชุดที่กำหนดใน issues (แม้ค่าจริงจะผิดเพี้ยนจาก GT บ่อย เช่นเดา "หมวดวิชาศึกษาทั่วไป" ผิดๆ แต่ยังอยู่ในชุดค่าที่อนุญาต ไม่ใช่โมเดลคิดชื่อหมวดใหม่) |
| 4 | ปี=0/ภาค=0 ต้องมี flexible_year_semester (และห้ามมีถ้าระบุปีชัดแล้ว) | **ไม่ผ่าน** | พบทั้งสองทิศทาง: (ก) `90644xxx` มี ปี/ภาค=0 แต่ไม่ใส่ flexible_year_semester (ข) `06026215`, `06026259`, `06026260` ระบุปีชัดเจนแล้วแต่ดันใส่ flexible_year_semester มาด้วย (ย้อนแย้งกับตัวเอง) — สาเหตุคือโมเดลสับสนระหว่าง "เลือกวิชาไหน" (วิชาเลือกที่ตายตัวว่าเรียนปี/ภาคไหน) กับ "เลือกเรียนเมื่อไหร่" (ยืดหยุ่นจริงๆ) ดูรายละเอียดในหัวข้อก่อนหน้า |
| 5 ⭐ | prerequisite ต้องอ้างถึงวิชาที่มีอยู่จริงในรายการ | **ผ่าน** (ไม่มี issue ประเภทนี้ในรายงาน) | ไม่พบ prerequisite ที่อ้างรหัสวิชาซึ่งไม่มีอยู่ในผลลัพธ์ 52 วิชานี้ — แปลว่าเท่าที่สกัดมา referential integrity ของ prerequisite ยังคงอยู่ (อาจเป็นเพราะ prerequisite ส่วนใหญ่ในหน้า 30-36 เป็น "ไม่มี" อยู่แล้ว จึงไม่มีโอกาสอ้างผิด) |

**สรุป: ผ่าน 3/5 กฎ (2, 3, 5) ไม่ผ่าน 2/5 กฎ (1, 4)** ทั้งสองกฎที่ไม่ผ่านสืบเนื่องมาจากปัญหาเดียวกันคือ
"การจัดหมวดวิชาเลือกที่มีทางเลือกซ้อนกันในตารางเดียว" (มัลติเพิลตัวเลือกภายใต้รหัสเดียว/ปีภาคเดียว) ซึ่งทั้ง
qwen3:4b และ qwen3:1.7b ยังจัดการได้ไม่สมบูรณ์เท่ากัน — เป็นจุดที่ควรปรับปรุง prompt เพิ่มเติมถ้าจะพัฒนาต่อ
(นอกเหนือขอบเขตที่อนุญาตให้แก้ในแล็บนี้ จึงบันทึกไว้เป็นข้อสังเกตเท่านั้น ไม่ได้แก้โค้ด)

**หมายเหตุ:** verify_internal ตรวจพบปัญหาได้ "โดยไม่ใช้เฉลย" จริงตามที่ใบงานออกแบบไว้ — ทั้ง 4 รายการที่ตรวจพบ
(xxxxxx สั้นไป, 90644xxx ขาด flexible, 06026215/259/260 มี flexible เกิน) ล้วนเป็นสิ่งที่มองออกได้จากโครงสร้าง
ข้อมูลเองล้วนๆ โดยไม่ต้องเปิด ground truth มาเทียบเลย ตรงกับหลักการ "Generate → Verify → Repair → Escalate"
ที่ใบงานอธิบายไว้ในส่วนที่ 7

---

## 2026-09-10 — ข้อ 8: เริ่มรันครบทุก pipeline (baseline+text+vlm) หน้า 30-36 → output/

**คำสั่งที่รันจริง (แบบ detached ด้วย nohup/disown เพื่อเลี่ยงปัญหา background task ถูก kill ก่อนเสร็จ
ตามที่บันทึกไว้ในข้อ 4 ด้านบน):**
```
nohup env PYTHONIOENCODING=utf-8 PYTHONUNBUFFERED=1 python -u lab7b_curriculum.py \
  --input data/DSBA_curriculum.pdf --pages "30-36" \
  --gt gt/DSBA_academic_plan_coop.json \
  --pipeline all --out output/ > output_run.log 2>&1 &
disown
```
ใช้ค่า default ทั้งหมด (LAB7_CHUNK=1, LAB7_MODEL_TEXT=qwen3:4b, DPI=150) — ไม่ใช้ค่าที่ทดลองในข้อ 6
เพราะข้อ 8 ต้องการผลเทียบ 3 pipeline แบบมาตรฐานตามใบงาน ไม่ใช่ผลจากการทดลองพารามิเตอร์

คาดว่าจะใช้เวลานานมาก (จากที่วัดได้ก่อนหน้า: vlm ~22 นาที/3หน้า, text ~2 ชม./7หน้า รวม baseline ที่เร็ว
→ รวมทั้งหมดอาจ 2-4+ ชั่วโมง) — ผู้ใช้ (veekritowatsarn.film@gmail.com) อนุญาตให้ทำงานต่อแบบไม่ต้องรอ confirm
ระหว่างที่ไม่อยู่หน้าจอ ~1 ชม. จึงปล่อยให้รันต่อเนื่องแบบ detached ต่อไปแม้เกินเวลาที่ผู้ใช้จะกลับมา
ถ้า chunk ไหนล้มเหลวครบ retry (เหมือนที่เจอในข้อ 4) จะแพตช์ด้วยวิธีเดียวกัน (รันหน้านั้นแยกด้วย qwen3:1.7b
แล้ว merge) แต่จะบันทึกไว้ชัดเจนว่าใช้โมเดลผสมเช่นเดียวกับข้อ 4 ถ้าเกิดกรณีนี้ขึ้นอีก

สิ่งที่ยังเหลือ: รอผลข้อ 8 (กำลังรันอยู่ ณ เวลาบันทึกนี้), สรุปผลสุดท้ายทั้งหมดเมื่อเสร็จ

---

## 2026-09-10 — ข้อ 8 อัปเดต: baseline+text เสร็จ (แพตช์ซ้ำแบบเดิม), vlm กำลังรัน

**baseline:** เหมือน Round 1 ทุกประการ — 0 วิชา (regex ไม่จับตารางที่ชื่อวิชาคนละบรรทัด) ไม่มี `pred_baseline.json`

**text:** เจอรูปแบบความล้มเหลวเดิมเป๊ะ — ก้อนที่ 1-4 (หน้า 30-33) สำเร็จ (27 วิชา, 6,395.6 วิ) ก้อนที่ 5, 6, 7
(หน้า 34, 35, 36) ล้มเหลวครบ retry ด้วย "โมเดลตอบว่าง" เหมือน Round 2 ทุกประการ (ยืนยันว่าไม่ใช่เรื่องบังเอิญ
แต่เป็นพฤติกรรม flaky จริงของ qwen3:4b ที่เจอซ้ำในการรันคนละรอบ)

**การแพตช์:** เนื่องจากเป็นหน้าเดียวกัน (34/35/36), model เดียวกัน (qwen3:1.7b), การตั้งค่าเดียวกันทุกอย่างกับที่
แพตช์ไปแล้วในข้อ 4 **จึงนำผลจาก `out_patch_page34/35/36` ที่มีอยู่แล้วมาใช้ซ้ำโดยตรง** (ไม่ต้องรันโมเดลใหม่ ประหยัดเวลา
~100 วิ และลดภาระ CPU ขณะที่ vlm pipeline กำลังรันคู่ขนานอยู่) หลังตรวจยืนยันแล้วว่าก้อนที่ 1-4 ของ `output/pred_text.json`
กับ `out_full/pred_text.json` เหมือนกันทุกตัวอักษร (courses[:4] เท่ากัน) จึงมั่นใจได้ว่า merge ถูกต้อง

**ผลลัพธ์ text (patched) ใน output/:** 52 วิชา, จับคู่วิชา 26/90, P=0.500 R=0.289 F1=0.366, CER รวม(micro)=0.849
(ใกล้เคียงกับ Round 2 มาก ต่างกันเล็กน้อยในหมวดวิชา/บังคับ-เลือกเพราะกลไก matching สุ่มจับคู่ต่างกันบ้างเมื่อมีวิชา
โค้ดซ้ำ (placeholder) หลายตัว — ไม่ใช่ความผิดปกติ) verify_internal ให้ผลเหมือน Round 2 ทุกประเด็น (ไม่ผ่านกฎ 1, 4)

**vlm:** กำลังรัน (ขั้น 1/2 Typhoon-OCR อ่านภาพ หน้า 1/7 ณ เวลาบันทึกนี้) — คาดว่าจะใช้เวลานาน
(Round 1 ใช้ ~22 นาที/3 หน้า → 7 หน้าอาจ ~50 นาที-1 ชั่วโมงขึ้นไป ถ้า Typhoon-OCR หรือ qwen3:4b ก้อนไหน
ล้มเหลวซ้ำแบบ text pipeline ก็อาจนานกว่านั้น)

สิ่งที่ยังเหลือ: รอ vlm จบ, ตรวจ intermediate_vlm.md ของหน้า 30-36 เต็ม (ไฟล์ใหม่จะทับของ Round 1 ที่มีแค่ 3 หน้า),
สรุปตาราง comparison.csv สุดท้ายที่มีครบ 3 pipeline (baseline ไม่มีแถวเพราะ 0 วิชา, text, vlm)

---

## 2026-09-10 — ข้อ 8 เสร็จสมบูรณ์: ผลสุดท้ายครบ 3 pipeline + บั๊กสำคัญที่พบระหว่างแพตช์ (แก้แล้ว)

**vlm จบแล้ว:** รวม 14 วิชา จาก 7 หน้า (elapsed 3,854.8 วิ ≈ 64 นาที) แยกตามหน้า:
หน้า30=7 (อ่านถูก), หน้า31=0 (Typhoon-OCR วนลูปซ้ำ garbage เหมือน Round1), หน้า32=0 (เหมือนกัน),
หน้า33=7 (อ่านถูก), หน้า34=0 (คราวนี้ Typhoon-OCR อ่านได้แต่ **qwen3:4b ขั้น jsonify ต่างหากที่ตอบว่าง** —
คนละจุดล้มเหลวจากหน้า31/32), หน้า35=ล้มเหลวที่ qwen3:4b เช่นกัน (ก้อนที่6), หน้า36=0
→ ยืนยันว่า vlm มีจุดล้มเหลวได้ 2 ชั้น: (1) OCR/Typhoon อ่านภาพพัง (หน้า31-32) และ (2) qwen3:4b jsonify
ตอบว่างแม้ markdown จะถูกต้อง (หน้า34-35) — เป็นปัญหาคนละสาเหตุ แต่ผลลัพธ์ปลายทางเหมือนกันคือ "ได้ 0 วิชา"

**⚠️ พบบั๊กในขั้นตอนแพตช์ของตัวเอง (ตรวจพบและแก้ไขแล้ว):** ตอนแรกรันสคริปต์ merge แพตช์ผลของ `output/pred_text.json`
"ระหว่าง" ที่ pipeline vlm ในรอบ `--pipeline all` เดียวกันยังไม่จบ (เพื่อประหยัดเวลาไม่ต้องรอ) แต่ปรากฏว่า
main() ของสคริปต์หลักจะคำนวณและ**เขียนทับ `output/comparison.csv` และ `output/evaluation.json` อีกครั้งเอง
ตอนจบการรันทั้งหมด** (หลัง vlm เสร็จ) โดยใช้ข้อมูล "text" ที่ค้างอยู่ในหน่วยความจำตั้งแต่ก่อนแพตช์ (27 วิชาดิบ)
ไม่ใช่ไฟล์ 52 วิชาที่แพตช์ไว้บนดิสก์ ทำให้ `evaluation.json` ที่ได้หลัง `--pipeline all` จบ **ย้อนกลับไปเป็นข้อมูล
ก่อนแพตช์โดยไม่ได้ตั้งใจ** (ตรวจพบเพราะ `internal_check.n_courses` กลับไปเป็น 27 ทั้งที่ไฟล์ pred_text.json บนดิสก์
มี 52 วิชาอยู่แล้ว — ตัวเลข CER ในบางคอลัมน์บังเอิญเท่ากันทั้งสองเวอร์ชันเพราะ 25 วิชาที่แพตช์เพิ่มมาไม่ได้จับคู่กับ
GT เพิ่มเลยสักแถว มีแค่ตัวเลข "แต่งเกิน/spurious" กับ P เท่านั้นที่ต่างกันชัดเจน (1 vs 26, P=0.963 vs 0.500)
ซึ่งเป็นจุดที่ทำให้สังเกตเห็นความผิดปกติ)

**วิธีแก้:** รันสคริปต์ merge เดิมซ้ำอีกครั้ง **หลังจากที่ `--pipeline all` จบสมบูรณ์แล้วเท่านั้น** คราวนี้เขียนทับ
`output/comparison.csv`/`evaluation.json` เป็นครั้งสุดท้ายจริงๆ (ไม่มีอะไรมาเขียนทับซ้ำอีกเพราะสคริปต์หลักจบงานไปแล้ว)
**บทเรียนสำหรับใครจะทำ pipeline แพตช์ลักษณะนี้อีก: ต้องรอให้ `--pipeline all` executable ตัวเดิมจบการทำงานทั้งหมด
ก่อนเสมอ ห้ามแพตช์ผลลัพธ์ระหว่างที่ยังมี pipeline อื่นในคำสั่งเดียวกันรันค้างอยู่** เพราะ main() จะเขียนทับไฟล์สรุปผล
ทั้งหมดใหม่เสมอตอนจบ (มีผลเฉพาะไฟล์สรุป comparison.csv/evaluation.json เท่านั้น ไฟล์ pred_<pipeline>.json รายพีชนีลไม่ถูกเขียนทับซ้ำ)

**ผลสุดท้ายที่ถูกต้องของข้อ 8 (`output/`, ครบ 3 pipeline, หน้า 30-36):**

| pipeline | วิชาที่ได้ | จับคู่กับ GT | P | R | F1 | CER รหัสวิชา | เวลารวม |
|---|---|---|---|---|---|---|---|
| baseline | 0 | 0/90 | — | 0.000 | — | 1.0 (ไม่มีข้อมูล) | เร็วมาก (<1 นาที) |
| text (patched) | 52 | 26/90 | 0.500 | 0.289 | 0.366 | 0.716 | ~6,396 วิ (qwen3:4b) + ~104 วิ (qwen3:1.7b แพตช์ 3 หน้า) ≈ 108 นาที |
| vlm | 14 | 7/90 | 0.500 | 0.078 | 0.135 | 0.923 | 3,854.8 วิ ≈ 64 นาที |

**ข้อสรุปสุดท้าย (ตอบคำถามหลักของแล็บ): pipeline `text` ยังคงชนะ vlm ในทุกมิติที่วัดได้แม้หลังแพตช์**
recall สูงกว่า (26 เทียบ 7), CER รหัสวิชาต่ำกว่า (0.716 เทียบ 0.923), และไม่มี pipeline ไหนพึ่ง baseline ได้เลย (0 วิชา)
ยืนยันข้อสรุปเดิมจาก Round 1/2: เอกสารนี้เป็น digital PDF แท้ๆ การข้าม OCR ไปดึงข้อความตรงเป็นทางเลือกที่ถูกต้อง
แม้ตัวเลข P/R/F1 โดยรวมจะดูไม่สูงมากสำหรับทั้งคู่ (เพราะปัญหาการจับคู่กับรหัส placeholder ซ้ำที่อธิบายไว้ก่อนหน้า
ไม่ใช่เพราะเนื้อหาอ่านผิดจริง) — ถ้าจะปรับปรุงต่อ จุดที่คุ้มค่าที่สุดคือแก้การจัดหมวด year/semester ของแถวที่มี
ตัวเลือกซ้อนกัน (กฎข้อ 4 ที่ตกในข้อ 7) ไม่ใช่การเปลี่ยน pipeline หรือโมเดล

**สรุปรวมทั้ง 8 ข้อในใบงาน: เสร็จครบทุกข้อแล้ว** (1-8 ✓) เหลือเพียงการเขียนรายงานสรุปจากข้อมูลใน PROGRESS.md นี้
